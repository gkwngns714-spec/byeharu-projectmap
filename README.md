# Byeharu — Project Map (published build)

A 3D map of the byeharu codebase — what exists, what connects to what, and what is
actually live in production — plus the 조직도 tree tab and the roadmap.

**This repo holds only the published static build.** The source tool lives in the
main game repo at `gkwngns714-spec/byeharu` → `tools/projectmap/` (branch
`feat-project-map`). Rebuild with `npm run refresh && npm run build` there.
